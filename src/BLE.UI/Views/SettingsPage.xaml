<?xml version="1.0" encoding="utf-8"?>
<uranium:UraniumContentPage
    x:Class="BLE.UI.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
    xmlns:viewModels="clr-namespace:BLE.UI.ViewModels"
    x:DataType="viewModels:SettingsViewModel"
    Title="{Binding Title}">

    <ScrollView>
        <VerticalStackLayout Spacing="24" Padding="24">
            <Label Text="Darstellung" FontSize="20" FontAttributes="Bold" />

            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto">
                <Label Text="Dunkles Theme verwenden" Grid.Column="0" VerticalOptions="Center" />
                <Switch
                    Grid.Column="1"
                    IsToggled="{Binding UseDarkTheme}"
                    Toggled="OnThemeToggled" />
            </Grid>

            <Label Text="Benutzerkonto" FontSize="20" FontAttributes="Bold" />

            <Button
                Text="Abmelden"
                StyleClass="FilledTonalButton"
                Command="{Binding SignOutCommand}" />
        </VerticalStackLayout>
    </ScrollView>
</uranium:UraniumContentPage>
