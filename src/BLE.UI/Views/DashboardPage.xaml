<?xml version="1.0" encoding="utf-8"?>
<uranium:UraniumContentPage
    x:Class="BLE.UI.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
    xmlns:viewModels="clr-namespace:BLE.UI.ViewModels"
    x:DataType="viewModels:DashboardViewModel"
    Title="{Binding Title}">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="24">

            <CollectionView
                ItemsSource="{Binding Metrics}"
                SelectionMode="None">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="2" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="viewModels:DashboardMetric">
                        <Frame
                            Margin="4"
                            Padding="16"
                            CornerRadius="16"
                            HasShadow="False">
                            <VerticalStackLayout Spacing="4">
                                <Label Text="{Binding Title}" FontAttributes="Bold" />
                                <Label Text="{Binding Value}" FontSize="24" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Label Text="Letzte Imports" FontSize="20" FontAttributes="Bold" />

            <CollectionView
                ItemsSource="{Binding RecentImports}"
                SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="viewModels:RecentImportItem">
                        <Frame Padding="16" Margin="0,4" HasShadow="False" CornerRadius="12">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="{Binding Filename}" FontAttributes="Bold" />
                                <Label Text="{Binding Source}" FontSize="12" />
                                <Label Text="{Binding Status}" FontSize="12" />
                                <Label Text="{Binding ImportedAt, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" FontSize="12" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</uranium:UraniumContentPage>
